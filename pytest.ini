[pytest]
asyncio_mode = auto
testpaths = tests/python
pythonpath = .
markers =
    integration: tests that touch a real database (must use the db_cursor fixture against TEST_DB_*; skipped by default; run with `pytest -m integration`)
addopts = -m "not integration"
